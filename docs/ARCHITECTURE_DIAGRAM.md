# 🏗️ ARCHITECTURE DU PROJET

---

## 📊 Architecture Générale

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT BROWSER                           │
│                    (http://localhost:49518)                      │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    ANGULAR FRONTEND                       │   │
│  │                                                            │   │
│  │  ┌─────────────┐  ┌──────────┐  ┌──────────────────┐    │   │
│  │  │  Components │  │ Services │  │  Guards/Interceptors│   │   │
│  │  │             │  │          │  │                  │    │   │
│  │  │ • Login     │  │ • API    │  │ • AuthGuard      │    │   │
│  │  │ • Register  │  │ • Error  │  │ • AuthInterceptor│    │   │
│  │  │ • Dashboard │  │          │  │                  │    │   │
│  │  │ • Data      │  │          │  │                  │    │   │
│  │  │ • Model     │  │          │  │                  │    │   │
│  │  │ • Training  │  │          │  │                  │    │   │
│  │  │ • Charts    │  │          │  │                  │    │   │
│  │  └─────────────┘  └──────────┘  └──────────────────┘    │   │
│  │                                                            │   │
│  │              localStorage (token, user)                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                    │
│                              │ HTTP/REST                          │
│                              ▼                                    │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │
                    ┌───────────┴───────────┐
                    │                       │
                    ▼                       ▼
        ┌─────────────────────┐  ┌──────────────────┐
        │   EXPRESS SERVER    │  │  EXTERNAL APIs   │
        │ (localhost:3000)    │  │                  │
        │                     │  │ • TensorFlow.js  │
        │ ┌─────────────────┐ │  │ • MongoDB        │
        │ │  Middleware     │ │  │ • Redis          │
        │ │                 │ │  │                  │
        │ │ • CORS          │ │  └──────────────────┘
        │ │ • Auth          │ │
        │ │ • Validation    │ │
        │ │ • Logging       │ │
        │ │ • Cache         │ │
        │ │ • Rate Limit    │ │
        │ └─────────────────┘ │
        │                     │
        │ ┌─────────────────┐ │
        │ │  Routes (API)   │ │
        │ │                 │ │
        │ │ • /api/auth     │ │
        │ │ • /api/model    │ │
        │ │ • /api/data     │ │
        │ │ • /api/training │ │
        │ │ • /api/files    │ │
        │ │ • /api/notif    │ │
        │ └─────────────────┘ │
        │                     │
        │ ┌─────────────────┐ │
        │ │  Services       │ │
        │ │                 │ │
        │ │ • Model Service │ │
        │ │ • Training Srv  │ │
        │ │ • Data Service  │ │
        │ │ • File Service  │ │
        │ │ • ML Services   │ │
        │ └─────────────────┘ │
        │                     │
        │ ┌─────────────────┐ │
        │ │  Config         │ │
        │ │                 │ │
        │ │ • Logger        │ │
        │ │ • MongoDB       │ │
        │ │ • Redis         │ │
        │ └─────────────────┘ │
        └─────────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
    ┌────────┐ ┌────────┐ ┌────────┐
    │MongoDB │ │ Redis  │ │ Files  │
    │Database│ │ Cache  │ │ System │
    └────────┘ └────────┘ └────────┘
```

---

## 🔄 FLUX D'AUTHENTIFICATION

```
┌──────────────────────────────────────────────────────────────┐
│                    LOGIN FLOW                                 │
└──────────────────────────────────────────────────────────────┘

1. FRONTEND (Login Component)
   ┌─────────────────────────────────┐
   │ User remplit formulaire         │
   │ • Email: user@example.com       │
   │ • Password: ****                │
   │ Clique "Se connecter"           │
   └─────────────────────────────────┘
                    │
                    ▼
2. API SERVICE
   ┌─────────────────────────────────┐
   │ Appelle apiService.login()      │
   │ POST /api/auth/login            │
   │ Body: {email, password}         │
   └─────────────────────────────────┘
                    │
                    ▼
3. HTTP INTERCEPTOR
   ┌─────────────────────────────────┐
   │ Ajoute headers                  │
   │ Content-Type: application/json  │
   │ (Pas de token pour login)       │
   └─────────────────────────────────┘
                    │
                    ▼
4. BACKEND (auth-complete.js)
   ┌─────────────────────────────────┐
   │ Reçoit requête                  │
   │ Valide email/password           │
   │ Cherche user dans DB            │
   │ Compare passwords (bcrypt)      │
   └─────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
        ▼                       ▼
    SUCCÈS                  ERREUR
    ┌──────────────┐      ┌──────────────┐
    │ Génère JWT   │      │ Retourne 401 │
    │ token        │      │ Unauthorized │
    │ Retourne:    │      └──────────────┘
    │ {            │            │
    │   token,     │            ▼
    │   user,      │      Frontend affiche
    │   message    │      message d'erreur
    │ }            │
    └──────────────┘
        │
        ▼
5. FRONTEND (Login Component)
   ┌──────────────────────────────┐
   │ Reçoit réponse               │
   │ Sauvegarde dans localStorage │
   │ • localStorage.token = JWT   │
   │ • localStorage.user = user   │
   └──────────────────────────────┘
        │
        ▼
6. ROUTER
   ┌──────────────────────────────┐
   │ Redirection vers /dashboard  │
   │ AuthGuard vérifie token      │
   │ Token existe → Accès OK      │
   └──────────────────────────────┘
        │
        ▼
7. DASHBOARD COMPONENT
   ┌──────────────────────────────┐
   │ Affiche dashboard            │
   │ Utilisateur connecté ✅      │
   └──────────────────────────────┘
```

---

## 🔐 FLUX DE REQUÊTE PROTÉGÉE

```
┌──────────────────────────────────────────────────────────────┐
│              PROTECTED REQUEST FLOW                           │
└──────────────────────────────────────────────────────────────┘

1. FRONTEND (Component)
   ┌─────────────────────────────────┐
   │ Appelle API protégée            │
   │ this.apiService.getModels()     │
   │ GET /api/model/summary          │
   └─────────────────────────────────┘
                    │
                    ▼
2. HTTP INTERCEPTOR (auth.interceptor.ts)
   ┌─────────────────────────────────┐
   │ Intercepte requête              │
   │ Récupère token du localStorage  │
   │ Ajoute Authorization header:    │
   │ "Authorization: Bearer <token>" │
   └─────────────────────────────────┘
                    │
                    ▼
3. BACKEND (auth middleware)
   ┌─────────────────────────────────┐
   │ Reçoit requête                  │
   │ Extrait token du header         │
   │ Vérifie signature JWT           │
   │ Extrait userId du token         │
   └─────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
        ▼                       ▼
    VALIDE                  INVALIDE
    ┌──────────────┐      ┌──────────────┐
    │ Ajoute req   │      │ Retourne 401 │
    │ .user = {    │      │ Unauthorized │
    │   userId,    │      └──────────────┘
    │   email      │            │
    │ }            │            ▼
    │ Continue     │      Frontend:
    │ vers route   │      • Supprime token
    └──────────────┘      • Supprime user
        │                 • Redirection
        ▼                   vers login
4. ROUTE HANDLER
   ┌──────────────────────────────┐
   │ Traite requête               │
   │ Accès à req.user.userId      │
   │ Récupère données de l'user   │
   │ Retourne réponse             │
   └──────────────────────────────┘
        │
        ▼
5. FRONTEND (Component)
   ┌──────────────────────────────┐
   │ Reçoit réponse               │
   │ Affiche données              │
   │ Mise à jour UI               │
   └──────────────────────────────┘
```

---

## 🎓 FLUX D'ENTRAÎNEMENT

```
┌──────────────────────────────────────────────────────────────┐
│              TRAINING FLOW                                    │
└──────────────────────────────────────────────────────────────┘

1. FRONTEND (Training Component)
   ┌─────────────────────────────────┐
   │ Utilisateur remplit formulaire  │
   │ • Epochs: 10                    │
   │ • Batch Size: 32                │
   │ • Learning Rate: 0.001          │
   │ Clique "Démarrer entraînement"  │
   └─────────────────────────────────┘
                    │
                    ▼
2. API SERVICE
   ┌─────────────────────────────────┐
   │ POST /api/training/start        │
   │ Body: {epochs, batchSize, ...}  │
   │ Header: Authorization: Bearer   │
   └─────────────────────────────────┘
                    │
                    ▼
3. BACKEND (trainingService)
   ┌─────────────────────────────────┐
   │ Reçoit requête                  │
   │ Charge modèle ML                │
   │ Charge données                  │
   │ Crée configuration              │
   └─────────────────────────────────┘
                    │
                    ▼
4. TENSORFLOW.JS
   ┌─────────────────────────────────┐
   │ Entraîne modèle                 │
   │ • Forward pass                  │
   │ • Calcul loss                   │
   │ • Backpropagation               │
   │ • Mise à jour poids             │
   │ Répète pour chaque epoch        │
   └─────────────────────────────────┘
                    │
                    ▼
5. BACKEND (trainingService)
   ┌─────────────────────────────────┐
   │ Collecte métriques              │
   │ • Loss par epoch                │
   │ • Accuracy par epoch            │
   │ • Temps d'entraînement          │
   │ Sauvegarde historique           │
   └─────────────────────────────────┘
                    │
                    ▼
6. FRONTEND (Training Component)
   ┌─────────────────────────────────┐
   │ Polling /api/training/history   │
   │ Récupère métriques              │
   │ Affiche graphiques              │
   │ Affiche progression             │
   └─────────────────────────────────┘
                    │
                    ▼
7. COMPLETION
   ┌─────────────────────────────────┐
   │ Entraînement terminé            │
   │ Affiche résultats finaux        │
   │ Sauvegarde modèle               │
   │ Utilisateur peut évaluer        │
   └─────────────────────────────────┘
```

---

## 📊 ARCHITECTURE DES DONNÉES

```
┌──────────────────────────────────────────────────────────────┐
│                    DATA FLOW                                  │
└──────────────────────────────────────────────────────────────┘

FRONTEND (localStorage)
├── token (JWT)
├── user (userId, email, username)
└── savedEmail (optionnel)

        │
        ▼

BACKEND (Memory/Session)
├── trainingHistory
├── modelCache
└── userSessions

        │
        ▼

MONGODB (Persistent)
├── Users Collection
│   ├── _id
│   ├── username
│   ├── email
│   ├── password (hashed)
│   └── profile
│
├── Datasets Collection
│   ├── _id
│   ├── userId
│   ├── name
│   ├── filePath
│   └── uploadedAt
│
├── Models Collection
│   ├── _id
│   ├── userId
│   ├── name
│   ├── architecture
│   └── metrics
│
└── Training Collection
    ├── _id
    ├── userId
    ├── modelId
    ├── history
    └── metrics

        │
        ▼

REDIS (Cache)
├── user:<userId> (TTL: 1h)
├── model:<modelId> (TTL: 24h)
├── training:<trainingId> (TTL: 7d)
└── cache:<key> (TTL: configurable)

        │
        ▼

FILE SYSTEM (Files)
├── uploads/
│   ├── datasets/
│   ├── models/
│   └── backups/
│
└── logs/
    ├── server.log
    ├── error.log
    └── access.log
```

---

## 🔌 INTÉGRATIONS EXTERNES

```
┌──────────────────────────────────────────────────────────────┐
│              EXTERNAL INTEGRATIONS                            │
└──────────────────────────────────────────────────────────────┘

FRONTEND
    │
    ├─→ Chart.js / ng2-charts
    │   └─ Visualisation graphiques
    │
    └─→ Angular Material (optionnel)
        └─ Composants UI

BACKEND
    │
    ├─→ TensorFlow.js
    │   └─ ML model training & prediction
    │
    ├─→ MongoDB
    │   └─ Persistent data storage
    │
    ├─→ Redis
    │   └─ Caching & sessions
    │
    ├─→ bcryptjs
    │   └─ Password hashing
    │
    ├─→ jsonwebtoken
    │   └─ JWT token generation
    │
    ├─→ multer
    │   └─ File uploads
    │
    ├─→ express-validator
    │   └─ Input validation
    │
    ├─→ winston
    │   └─ Logging
    │
    └─→ morgan
        └─ HTTP logging
```

---

## 🚀 DÉPLOIEMENT ARCHITECTURE

```
┌──────────────────────────────────────────────────────────────┐
│              PRODUCTION DEPLOYMENT                            │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    CDN / LOAD BALANCER                       │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
    ┌────────┐          ┌────────┐         ┌────────┐
    │Server 1│          │Server 2│         │Server 3│
    │(Node)  │          │(Node)  │         │(Node)  │
    └────────┘          └────────┘         └────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
    ┌────────┐          ┌────────┐         ┌────────┐
    │MongoDB │          │ Redis  │         │Storage │
    │Cluster │          │Cluster │         │(S3/GCS)│
    └────────┘          └────────┘         └────────┘
```

---

**Créé le:** 1 Décembre 2025
**Status:** ✅ ARCHITECTURE DOCUMENTÉE

